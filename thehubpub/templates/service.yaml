apiVersion: v1
kind: Service
metadata:
  name: {{ include "hubpub.fullname" . }}
spec:
  ports:
  - name: "tcp-{{ include "hubpub.fullname" . }}"
    port: {{ .Values.containerPort }}
    targetPort: {{ .Values.containerPort }}
  selector:
    app: {{ include "hubpub.fullname" . }}
