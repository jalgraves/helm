appName: menu-api

image:
  repository: jalgraves/menu_api
  pullPolicy: Always
  # Overrides the image tag whose default is the chart appVersion.
  tag: ""

containerPort: 5004
ingressHost: menu-api.jalgraves.com

db:
  dbName: food
  host: postgres.default.svc.cluster.local
  port: 5432
