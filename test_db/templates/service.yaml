apiVersion: v1
kind: Service
metadata:
  name: {{ include "test-db.fullname" . }}
  labels:
    {{- include "test-db.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "test-db.selectorLabels" . | nindent 4 }}
